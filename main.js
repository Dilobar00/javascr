const text = "jumavoy qalesan dostim ishlaring yaxshimi"
let result = text.split(" ")
  

const firstChar = text.charAt().toUpperCase();
const restChar = text.slice(1);
const end = firstChar + restChar;

console.log(end);
